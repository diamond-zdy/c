#define _CRT_SECURE_NO_WARNINGS 1
实现一个函数，打印乘法口诀表，口诀表的行数和列数自己指定，
输入9，输出9 * 9口诀表，输入12，输出12 * 12的乘法口诀表。
#include <stdio.h>
#include <math.h>
void  mul(int n1)//创建函数
{
	int a = 0;
	int b = 0;

	for (a = 1; a <= n1; a++)
	{
		for (b = 1; b <= a; b++)
			printf("%d*%d=%d  ", a, b, a*b);
		printf("\n");
	}

}
int main()
{
	int n = 0;
	printf("请输入数字： ");
	scanf("%d", &n);
	mul(n);
}
