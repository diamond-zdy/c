#include <stdio.h>

int main()
{
	
	int s = 100;
	for (s = 100; s <= 999; s++)
	{
		int a = s / 100;
		int b = s / 10 % 10;
		int c = s % 10;
		if (a*a*a + b*b*b + c*c*c == s)
		{
			printf("%d ", s);
		}
	}
	return 0;
}
