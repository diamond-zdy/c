//3.编写代码模拟三次密码输入的场景。 
//最多能输入三次密码，密码正确，提示“登录成功”, 密码错误，
//可以重新输入，最多输入三次。三次均错，则提示退出程序。
#include <stdio.h>
#include <windows.h>
#include <string.h>
int main()
{
	int i = 0;
	char arr1[10] = { 123456 };
	char arr2[10] = { 0 };
	for (i = 1; i <= 3; i++)
	{
		printf("请输入密码：>\n");
		gets(arr2);
		if (0 == strcmp(arr1,arr2))
		{
			printf("密码正确");
			system("pause");
		}
		else
		{
			printf("密码错误，请重新输入");
			system("pause");
		}
	}
	printf("退出程序");
	return 0;
}
